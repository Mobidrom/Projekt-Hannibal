name: "SEVAS Konversion"

on:
  workflow_dispatch:
    inputs:
      osm_url:
        description: "URL zu einer OSM PBF Datei, z. B. http://download.geofabrik.de/europe/germany/nordrhein-westfalen-latest.osm.pbf"
        required: true
        default: "https://download.geofabrik.de/europe/germany/nordrhein-westfalen-latest.osm.pbf"
      sevas_url:
        description: "URL zum SEVAS WFS Client"
        required: true
        default: "https://sevas.nrw.de/osm/sevas"
      clean_tags:
        description: "Durch Leerzeichen separierte tags, die innerhalb der in 'clean_area' angegebenen Fläche von allen Objekten entfernt werden"
        required: false
        default: ""
      clean_area:
        description: "OSM ID einer Relation, die eine Fläche darstellt. Wenn angegeben, werden innerhalb dieser Fläche alle Tags mit in 'clean_tags' angegebenen Keys entfernt"
        required: false
        default: "-1"
      file_name:
        description: "Name der finalen OSM PBF Datei (ohne Endung)"
        required: false
        default: "sevas"

jobs:
  convert:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v3

      - name: Obtain OSM file checksum
        id: obtain_checksum
        run: |
          CHECKSUM_URL=${{ inputs.osm_url }}.md5
          if data="$(curl -fsL $CHECKSUM_URL)"; then
            echo "checksum=$(echo "${data}" | awk '{print $1"-"$2}')" >> $GITHUB_OUTPUT
          fi

      - uses: actions/cache@v3
        name: Cache OSM File
        id: cache-osm-file
        with:
          path: ~/.osm_source
          key: osm-checksum-${{ steps.obtain_checksum.outputs.checksum }}

      - name: Install required packages
        run: |
          sudo apt-get update --yes
          sudo apt-get install --yes --quiet libgdal-dev osmium-tool

      - name: Install uv
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          enable-cache: true

      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version-file: "pyproject.toml"

      - name: Install dependencies
        run: uv sync --locked

      # TODO Still used?
      - uses: actions/cache@v4
        with:
          path: ~/.cache/pre-commit
          key: pre-commit-${{ hashFiles('.pre-commit-config.yaml') }}

      - name: Download OSM File
        if: steps.cache-osm-file.outputs.cache-hit != 'true'
        run: |
          mkdir ~/.osm_source
          echo ${{ inputs.osm_url }}
          curl -sfLo ~/.osm_source/source.pbf ${{ inputs.osm_url }}

      - name: SEVAS Konversion
        run: |
          mkdir sevas_data

          uv run sevas_utils download sevas_data ${{ inputs.sevas_url }}
          uv run sevas_utils convert sevas_data ~/.osm_source/source.pbf ${{ inputs.file_name }}.pbf --clean-tags "${{ inputs.clean_tags }}"  --clean-area "${{ inputs.clean_area }}"
        # env:
        #   GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Archiviere konvertierte OSM Datei
        uses: actions/upload-artifact@v4
        with:
          name: sevas-osm
          path: ${{ inputs.file_name }}.pbf
